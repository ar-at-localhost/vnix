--TODO: Activate
local wezterm = require("wezterm")

wezterm.on("vnix:rename-workspace", function(win, pane) end)
wezterm.on("vnix:rename-tab", function(win, pane) end)
wezterm.on("vnix:rename-pane", function(win, pane) end)
